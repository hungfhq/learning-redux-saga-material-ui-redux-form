const styles = () => ({
  globalLoading: {
    position: 'fixed',
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
    left: 0,
    right: 0,
    top: 0,
    bottom: 0,
    zIndex: 99,
    background: 'rgba(0, 0, 0, 0.4)'
  },
  loadingText: {
    color: '#fff'
  },
})

export default styles
